# Copia este archivo a .env y rellena los valores.
# No subas .env al repositorio (contiene la contraseña).

# Servidor de correo saliente (approx.es)
# SSL requerido, puerto 465. Autenticación requerida.
SMTP_HOST=smtp.approx.es
SMTP_PORT=465
SMTP_USER=joseantonio@approx.es
SMTP_PASSWORD=tu_contraseña_de_correo
EMAIL_FROM=joseantonio@approx.es

# Opcional: dominio permitido para registro (por defecto approx.es)
# ALLOWED_EMAIL_DOMAIN=approx.es

# Opcional: JWT (cambiar en producción)
# JWT_SECRET=garantia-sat-secret-cambiar-en-produccion

# Desarrollo: devolver el código de verificación en la respuesta del registro (no envía correo)
# DEV_EMAIL_CODE_IN_RESPONSE=1

# Ruta del Excel para sincronizar (por defecto: productos.xlsx del proyecto). En producción: ruta al fichero en QNAP.
# EXCEL_SYNC_PATH=/ruta/al/productos.xlsx

# Para acceso desde otros PCs de la red (despliegue Windows): permitir cualquier origen.
# CORS_ORIGINS=*
